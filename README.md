# wheat

A simple implementation of Fusee Launcher for T124/T132 devices

## Build
### Archlinux
```shell
sudo pacman -S arm-none-eabi-newlib arm-none-eabi-gcc arm-none-eabi-binutils #Build Toolchain
sudo pacman -S libusb #gousb need this
```